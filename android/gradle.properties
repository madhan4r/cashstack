org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Works around a Kotlin incremental-compiler crash on Windows when the
# project and the Gradle/pub caches live on different drive letters (the
# incremental cache tries to compute a relative path across drive roots,
# which throws). See https://youtrack.jetbrains.com/issue/KT-58000-family.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
